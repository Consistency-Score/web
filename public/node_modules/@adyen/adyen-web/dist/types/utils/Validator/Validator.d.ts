declare class Validator {
    constructor(rules?: {});
    rules: {
        input: {
            default: () => boolean;
        };
        blur: {
            shopperEmail: (email: any) => boolean;
            default: () => boolean;
        };
    };
    setRules(newRules: any): void;
    validate(field: any, mode?: string): (value: any) => any;
}
export default Validator;
