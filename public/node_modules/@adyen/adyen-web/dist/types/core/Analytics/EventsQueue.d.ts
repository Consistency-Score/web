declare type EventItem = (conversionId: string) => Promise<any>;
declare class EventsQueue {
    events: EventItem[];
    add(event: any): void;
    run(conversionId?: string): Promise<any[]>;
}
export default EventsQueue;
