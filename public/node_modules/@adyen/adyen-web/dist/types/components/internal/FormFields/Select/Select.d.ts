import { Component, h } from 'preact';
import './Select.scss';
import { SelectProps, SelectState } from './types';
declare class Select extends Component<SelectProps, SelectState> {
    private selectContainer;
    private toggleButton;
    private dropdownList;
    static defaultProps: {
        items: any[];
        readonly: boolean;
        onChange: () => void;
    };
    constructor(props: any);
    componentDidMount(): void;
    componentWillUnmount(): void;
    handleClickOutside(e: any): void;
    toggle(e: any): void;
    select(e: any): void;
    /**
     * Closes the dropdown and focuses the button element
     */
    closeDropdown(): void;
    /**
     * Handle keyDown events on the adyen-checkout__dropdown__element
     * Navigates through the list, or select an element, or close the menu.
     * @param e - KeyDownEvent
     */
    handleKeyDown(e: any): void;
    /**
     * Handle keyDown events on the adyen-checkout__dropdown__button
     * Opens the dropdownList and focuses the first element if available
     * @param e - KeyDownEvent
     */
    handleButtonKeyDown(e: any): void;
    handleOnError(e: any): void;
    render({ className, classNameModifiers, isInvalid, items, placeholder, readonly, selected }: {
        className?: string;
        classNameModifiers?: any[];
        isInvalid: any;
        items?: any[];
        placeholder: any;
        readonly: any;
        selected: any;
    }, { toggleDropdown }: {
        toggleDropdown: any;
    }): h.JSX.Element;
}
export default Select;
