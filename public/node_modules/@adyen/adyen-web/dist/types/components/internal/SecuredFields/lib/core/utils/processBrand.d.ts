import { SFFeedbackObj } from '../../types';
interface BrandInfoObject {
    brand: string;
    hideCVC: boolean;
    cvcRequired: boolean;
    cvcText: string;
}
export declare function handleProcessBrand(pFeedbackObj: SFFeedbackObj): BrandInfoObject;
export {};
