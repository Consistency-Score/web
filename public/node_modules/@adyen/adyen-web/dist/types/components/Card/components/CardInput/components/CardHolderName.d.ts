import { h } from 'preact';
import { CardHolderNameProps } from './types';
export default function CardHolderName({ onChange, placeholder, value, required, error, isValid }: CardHolderNameProps): h.JSX.Element;
