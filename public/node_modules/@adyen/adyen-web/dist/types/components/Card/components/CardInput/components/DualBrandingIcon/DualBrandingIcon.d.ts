import { h } from 'preact';
import { DualBrandingIconProps } from '../types';
import './DualBrandingIcon.scss';
declare const DualBrandingIcon: ({ brand, onClick, dataValue, notSelected }: DualBrandingIconProps) => h.JSX.Element;
export default DualBrandingIcon;
