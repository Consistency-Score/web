import { h } from 'preact';
import { CardFieldsProps } from './types';
export default function CardFields({ brand, cvcRequired, dualBrandingElements, dualBrandingChangeHandler, dualBrandingSelected, errors, focusedElement, hasCVC, hideCVCForBrand, onFocusField, showBrandIcon, valid }: CardFieldsProps): h.JSX.Element;
